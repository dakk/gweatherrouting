name: Windows Build

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
  release:
    types: [created]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    name: Windows Portable ZIP

    steps:
      - uses: actions/checkout@v4

      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            git
            mingw-w64-x86_64-gtk3
            mingw-w64-x86_64-python
            mingw-w64-x86_64-python-pip
            mingw-w64-x86_64-python-gobject
            mingw-w64-x86_64-gobject-introspection
            mingw-w64-x86_64-gdal
            mingw-w64-x86_64-python-gdal
            mingw-w64-x86_64-python-numpy
            mingw-w64-x86_64-python-matplotlib
            mingw-w64-x86_64-python-pyproj
            mingw-w64-x86_64-gdk-pixbuf2
            mingw-w64-x86_64-adwaita-icon-theme

      - name: Install Python Dependencies
        shell: msys2 {0}
        run: |
          pip install --break-system-packages pyinstaller
          pip install --break-system-packages requests colorlog geojson_utils bs4 pyserial pynmea2 gpxpy nmeatoolkit weatherrouting
          pip install --break-system-packages --no-build-isolation --no-deps .

      - name: Build Windows Executable
        shell: msys2 {0}
        run: |
          cd gweatherrouting
          pyinstaller --name GWeatherRouting \
            --windowed \
            --icon ../windows/icon.ico \
            --hidden-import=gi \
            --collect-submodules=gi \
            --add-data "data;gweatherrouting/data" \
            --add-data "gtk;gweatherrouting/gtk" \
            --runtime-hook ../windows/pyinstaller_gi_hook_win.py \
            $(for typelib in Gtk-3.0 Gdk-3.0 GdkPixbuf-2.0 Pango-1.0 PangoCairo-1.0 GObject-2.0 GLib-2.0 Gio-2.0 cairo-1.0 Atk-1.0 HarfBuzz-0.0 freetype2-2.0 GModule-2.0 GdkWin32-3.0; do
              typelibfile="/mingw64/lib/girepository-1.0/${typelib}.typelib"
              if [ -f "$typelibfile" ]; then
                winpath=$(cygpath -w "$typelibfile")
                echo "--add-data ${winpath};gi_typelibs"
              fi
            done) \
            __main__.py

      - name: Copy osgeo (GDAL Python bindings) and GDAL DLLs
        shell: msys2 {0}
        run: |
          DIST="gweatherrouting/dist/GWeatherRouting"
          OSGEO_DIR=$(python -c "import osgeo; import os; print(os.path.dirname(osgeo.__file__))")
          echo "Copying osgeo from $OSGEO_DIR"
          cp -r "$OSGEO_DIR" "$DIST/osgeo"

          # Copy GDAL and PROJ DLLs needed by _gdal.pyd etc.
          cp /mingw64/bin/libgdal-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libproj-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libgeos*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libsqlite3-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libtiff-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libcurl-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libgeotiff-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libjpeg-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libpng*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libopenjp2-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libwebp-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libxml2-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libexpat-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/zlib*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/liblzma-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libzstd*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libiconv-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libintl-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libssl-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libcrypto-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libbrotli*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libnghttp2-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libidn2-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libpsl-*.dll "$DIST/" 2>/dev/null || true
          cp /mingw64/bin/libssh2-*.dll "$DIST/" 2>/dev/null || true

      - name: Copy GTK runtime files
        shell: msys2 {0}
        run: |
          DIST="gweatherrouting/dist/GWeatherRouting"

          # GTK schemas
          mkdir -p "$DIST/share/glib-2.0/schemas"
          cp -r /mingw64/share/glib-2.0/schemas/* "$DIST/share/glib-2.0/schemas/"

          # Icons
          mkdir -p "$DIST/share/icons"
          cp -r /mingw64/share/icons/Adwaita "$DIST/share/icons/"
          cp -r /mingw64/share/icons/hicolor "$DIST/share/icons/"

          # Pixbuf loaders
          mkdir -p "$DIST/lib/gdk-pixbuf-2.0"
          cp -r /mingw64/lib/gdk-pixbuf-2.0/* "$DIST/lib/gdk-pixbuf-2.0/"

          # GDAL data
          if [ -d /mingw64/share/gdal ]; then
            mkdir -p "$DIST/share/gdal"
            cp -r /mingw64/share/gdal/* "$DIST/share/gdal/"
          fi

      - name: Upload Windows ZIP
        uses: actions/upload-artifact@v4
        with:
          name: GWeatherRouting-win64
          path: gweatherrouting/dist/GWeatherRouting/
